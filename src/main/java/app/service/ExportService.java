package app.service;

import org.springframework.stereotype.Service;

/**
 * Export functions
 */
@Service
public class ExportService {

    /*
    Be able to restrict the epochs for which data are exported (e.g. specify to export up to the first 36 hours of available data, but truncate data thereafter).
    Be able to specify which columns are exported (e.g. I10_*, I10_2 only, all data, etc)
    Be able to export down sampled data (e.g. hourly mean, median, variance, etc)
    */


}
